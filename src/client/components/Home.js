import React from 'react';

const Home = () => {
    return (
        <div>
        <div>Home Components</div>
        <button onClick={()=> console.log('here')}>Press me!</button>
        </div>
    )
};

export default Home;
